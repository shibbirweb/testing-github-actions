name: Check code style and run test cases

on:
  push:
    branches:
      - master
      - uat
      - dev
  pull_request:
    branches:
      - master
      - uat
      - dev

jobs:
  linting:
    name: Check code style and run test cases
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4    
      - uses: actions/cache@v4
        with:
          path: readme.md
          key: ${{ runner.os }}-font-abc
          restore-keys: |
            ${{ runner.os }}-font-
